//通过调用该时间，可以将时间进行格式化，已小时，天，分等显示。
	var itemSelectFuntion = function(){
	//NodeID//Nodetitle
     /* alert(this.id + "," + this.innerHTML);*/
    };
    $.selectSuggest('dialogInput',rows,itemSelectFuntion);